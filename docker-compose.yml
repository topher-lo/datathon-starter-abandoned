version: "3.8"
services:
    app:
        restart: always
        image: streamlit-e2e-boilerplate
        build:
            dockerfile: ./Dockerfile
            context: .
        volumes:
          - .:/home/app/webapp
        environment:
            - PORT=${PORT}
        ports:
            - ${PORT}:${PORT}